# Rust build artifacts (target-dir is redirected to D: via .cargo/config.toml).
/target/

# llvm-cov raw profiles
*.profraw

# local dogfooding sessions (tabit CLI run inside this repo)
/.tabit/
.zcode/
